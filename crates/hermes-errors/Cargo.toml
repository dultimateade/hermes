[package]
name = "hermes-errors"
version = "0.1.0"
edition = "2021"
description = "Canonical error definitions for all Hermes contracts"

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }
serde_json = { version = "1.0", optional = true }

[features]
default = []
serde = ["dep:serde", "dep:serde_json"]

[lib]
